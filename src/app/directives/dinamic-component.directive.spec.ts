import { DinamicComponentDirective } from './dinamic-component.directive';

describe('DinamicComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicComponentDirective();
    expect(directive).toBeTruthy();
  });
});
